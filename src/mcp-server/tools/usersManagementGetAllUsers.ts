/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersManagementGetAllUsers } from "../../funcs/usersManagementGetAllUsers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllUsersRequest$inboundSchema,
};

export const tool$usersManagementGetAllUsers: ToolDefinition<typeof args> = {
  name: "users-management-get-all-users",
  description: `Get all Users

To get all Users on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersManagementGetAllUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
