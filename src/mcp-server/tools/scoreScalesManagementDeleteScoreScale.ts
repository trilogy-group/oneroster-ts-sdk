/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scoreScalesManagementDeleteScoreScale } from "../../funcs/scoreScalesManagementDeleteScoreScale.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteScoreScaleRequest$inboundSchema,
};

export const tool$scoreScalesManagementDeleteScoreScale: ToolDefinition<
  typeof args
> = {
  name: "score-scales-management-delete-score-scale",
  description: `Delete a Score Scale

Perform a soft delete on a specific ScoreScale on the service provider. This operation changes the status of the ScoreScale to 'tobedeleted'.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scoreScalesManagementDeleteScoreScale(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
