/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demographicsManagementGetAllDemographics } from "../../funcs/demographicsManagementGetAllDemographics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllDemographicsRequest$inboundSchema,
};

export const tool$demographicsManagementGetAllDemographics: ToolDefinition<
  typeof args
> = {
  name: "demographics-management-get-all-demographics",
  description: `Get all Demographic records

To get all Demographic records on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demographicsManagementGetAllDemographics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
