/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { courseResourcesDelete } from "../../funcs/courseResourcesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveResourceFromCourseRequest$inboundSchema,
};

export const tool$courseResourcesDelete: ToolDefinition<typeof args> = {
  name: "course-resources-delete",
  description: `Remove a resource from a course

Removes the specified resource from the course.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await courseResourcesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
