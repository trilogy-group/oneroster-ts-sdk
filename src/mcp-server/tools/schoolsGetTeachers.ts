/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { schoolsGetTeachers } from "../../funcs/schoolsGetTeachers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTeachersForSchoolRequest$inboundSchema,
};

export const tool$schoolsGetTeachers: ToolDefinition<typeof args> = {
  name: "schools-get-teachers",
  description: `Get all teachers for a school

Returns a collection of teachers assigned to a specific school`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await schoolsGetTeachers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
