/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { courseClassesGetAll } from "../../funcs/courseClassesGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetClassesForCourseRequest$inboundSchema,
};

export const tool$courseClassesGetAll: ToolDefinition<typeof args> = {
  name: "course-classes-get-all",
  description: `Get Classes For Course

Returns all classes associated with a specific course`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await courseClassesGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
