/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { gradingPeriodsManagementDeleteGradingPeriod } from "../../funcs/gradingPeriodsManagementDeleteGradingPeriod.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteGradingPeriodRequest$inboundSchema,
};

export const tool$gradingPeriodsManagementDeleteGradingPeriod: ToolDefinition<
  typeof args
> = {
  name: "grading-periods-management-delete-grading-period",
  description: `Delete a Grading Period

Perform a soft delete on a specific Grading Period on the service provider. The operation changes the status of the Grading Period to 'tobedeleted'.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await gradingPeriodsManagementDeleteGradingPeriod(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
