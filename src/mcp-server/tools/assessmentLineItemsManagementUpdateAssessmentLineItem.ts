/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assessmentLineItemsManagementUpdateAssessmentLineItem } from "../../funcs/assessmentLineItemsManagementUpdateAssessmentLineItem.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAssessmentLineItemRequest$inboundSchema,
};

export const tool$assessmentLineItemsManagementUpdateAssessmentLineItem:
  ToolDefinition<typeof args> = {
    name: "assessment-line-items-management-update-assessment-line-item",
    description: `Update an Assessment Line Item

To update an existing Assessment Line Item. The sourcedId for the record to be updated is supplied by the requesting system.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await assessmentLineItemsManagementUpdateAssessmentLineItem(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
