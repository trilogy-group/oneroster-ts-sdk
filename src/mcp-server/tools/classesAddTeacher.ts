/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesAddTeacher } from "../../funcs/classesAddTeacher.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AddTeacherToClassRequest$inboundSchema,
};

export const tool$classesAddTeacher: ToolDefinition<typeof args> = {
  name: "classes-add-teacher",
  description: `Add a teacher to a class

Assigns a teacher to a specific class`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classesAddTeacher(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
