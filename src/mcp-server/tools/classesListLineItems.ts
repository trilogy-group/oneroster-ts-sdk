/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesListLineItems } from "../../funcs/classesListLineItems.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLineItemsForClassRequest$inboundSchema,
};

export const tool$classesListLineItems: ToolDefinition<typeof args> = {
  name: "classes-list-line-items",
  description: `Get line items for a class

Returns a collection of line items associated with a specific class.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classesListLineItems(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
