/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { termsManagementGetClassesForTerm } from "../../funcs/termsManagementGetClassesForTerm.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetClassesForTermRequest$inboundSchema,
};

export const tool$termsManagementGetClassesForTerm: ToolDefinition<
  typeof args
> = {
  name: "terms-management-get-classes-for-term",
  description: `Get Classes for a Term

To get the set of Classes related to a specific Term. If the specified term cannot be identified within the service provider, the api will return a 404 error code and message 'Term not found.'`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await termsManagementGetClassesForTerm(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
