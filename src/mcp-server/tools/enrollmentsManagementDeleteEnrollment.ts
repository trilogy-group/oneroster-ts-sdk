/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { enrollmentsManagementDeleteEnrollment } from "../../funcs/enrollmentsManagementDeleteEnrollment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteEnrollmentRequest$inboundSchema,
};

export const tool$enrollmentsManagementDeleteEnrollment: ToolDefinition<
  typeof args
> = {
  name: "enrollments-management-delete-enrollment",
  description: `Delete an Enrollment

Perform a soft delete on a specific Enrollment on the service provider. The operation changes the status of the Enrollment to 'tobedeleted'.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await enrollmentsManagementDeleteEnrollment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
