/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { studentsClassesGet } from "../../funcs/studentsClassesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetClassesForStudentRequest$inboundSchema,
};

export const tool$studentsClassesGet: ToolDefinition<typeof args> = {
  name: "students-classes-get",
  description: `Get classes for a student

Returns a collection of classes that the student is enrolled in.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await studentsClassesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
