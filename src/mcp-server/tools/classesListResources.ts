/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesListResources } from "../../funcs/classesListResources.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetResourcesForClassRequest$inboundSchema,
};

export const tool$classesListResources: ToolDefinition<typeof args> = {
  name: "classes-list-resources",
  description: `Get resources for a class

Returns a collection of resources associated with the class.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classesListResources(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
