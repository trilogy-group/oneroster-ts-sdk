/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesUsersManagementGetResourcesForUser } from "../../funcs/resourcesUsersManagementGetResourcesForUser.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetResourcesForUserRequest$inboundSchema,
};

export const tool$resourcesUsersManagementGetResourcesForUser: ToolDefinition<
  typeof args
> = {
  name: "resources-users-management-get-resources-for-user",
  description: `Get resources for a user

To get the collection of resources available to a specific user. If the corresponding record cannot be located, the api will return a 404 error code and message 'User not found.'`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesUsersManagementGetResourcesForUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
