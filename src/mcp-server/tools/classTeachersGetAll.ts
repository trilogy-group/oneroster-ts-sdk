/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classTeachersGetAll } from "../../funcs/classTeachersGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTeachersForClassRequest$inboundSchema,
};

export const tool$classTeachersGetAll: ToolDefinition<typeof args> = {
  name: "class-teachers-get-all",
  description: `Get teachers for a class

Returns a collection of teachers assigned to a specific class`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classTeachersGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
