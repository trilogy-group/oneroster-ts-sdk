/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { termsGet } from "../../funcs/termsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTermRequest$inboundSchema,
};

export const tool$termsGet: ToolDefinition<typeof args> = {
  name: "terms-get",
  description: `Get a specific term

Returns a specific term by sourcedId.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await termsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
