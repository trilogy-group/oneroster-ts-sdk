/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { coursesCreate } from "../../funcs/coursesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCourseRequest$inboundSchema.optional(),
};

export const tool$coursesCreate: ToolDefinition<typeof args> = {
  name: "courses-create",
  description: `Create Course

Used when creating a new course`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await coursesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
