/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesResultsGetForLineItem } from "../../funcs/classesResultsGetForLineItem.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetResultsForLineItemForClassRequest$inboundSchema,
};

export const tool$classesResultsGetForLineItem: ToolDefinition<typeof args> = {
  name: "classes-results-get-for-line-item",
  description: `Get results for a specific line item in a class

Returns a collection of results for a specific line item within a class.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classesResultsGetForLineItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
