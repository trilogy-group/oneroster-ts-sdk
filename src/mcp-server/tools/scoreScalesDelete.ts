/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scoreScalesDelete } from "../../funcs/scoreScalesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteScoreScaleRequest$inboundSchema,
};

export const tool$scoreScalesDelete: ToolDefinition<typeof args> = {
  name: "score-scales-delete",
  description: `Delete a score scale

Deletes a score scale with the specified sourcedId.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scoreScalesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
