/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { coursesGetComponent } from "../../funcs/coursesGetComponent.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCourseComponentRequest$inboundSchema,
};

export const tool$coursesGetComponent: ToolDefinition<typeof args> = {
  name: "courses-get-component",
  description: `Get Course Component

Returns a specific course component by sourcedId`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await coursesGetComponent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
