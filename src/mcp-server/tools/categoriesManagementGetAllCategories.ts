/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { categoriesManagementGetAllCategories } from "../../funcs/categoriesManagementGetAllCategories.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllCategoriesRequest$inboundSchema,
};

export const tool$categoriesManagementGetAllCategories: ToolDefinition<
  typeof args
> = {
  name: "categories-management-get-all-categories",
  description: `Get all Categories

Get all of the Line Item Categories on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await categoriesManagementGetAllCategories(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
