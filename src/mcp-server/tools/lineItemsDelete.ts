/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lineItemsDelete } from "../../funcs/lineItemsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteLineItemRequest$inboundSchema,
};

export const tool$lineItemsDelete: ToolDefinition<typeof args> = {
  name: "line-items-delete",
  description: `Delete a line item

Deletes a line item with the specified sourcedId.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lineItemsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
