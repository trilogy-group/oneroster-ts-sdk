/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesCreateLineItem } from "../../funcs/classesCreateLineItem.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostLineItemsForClassRequest$inboundSchema,
};

export const tool$classesCreateLineItem: ToolDefinition<typeof args> = {
  name: "classes-create-line-item",
  description: `Create a line item for a class

Creates a new line item associated with a specific class.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classesCreateLineItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
