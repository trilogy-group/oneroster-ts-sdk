/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesManagementAddStudentToClass } from "../../funcs/classesManagementAddStudentToClass.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AddStudentToClassRequest$inboundSchema,
};

export const tool$classesManagementAddStudentToClass: ToolDefinition<
  typeof args
> = {
  name: "classes-management-add-student-to-class",
  description: `Add a student to a Class

Enrolls a student in a specific Class. The responding system must return the set of sourcedIds that have been allocated to the newly created enrollment record.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classesManagementAddStudentToClass(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
