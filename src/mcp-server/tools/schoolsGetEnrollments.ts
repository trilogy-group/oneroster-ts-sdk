/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { schoolsGetEnrollments } from "../../funcs/schoolsGetEnrollments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetEnrollmentsForSchoolRequest$inboundSchema,
};

export const tool$schoolsGetEnrollments: ToolDefinition<typeof args> = {
  name: "schools-get-enrollments",
  description: `Get all enrollments for a school

Returns a collection of enrollments for a specific school`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await schoolsGetEnrollments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
