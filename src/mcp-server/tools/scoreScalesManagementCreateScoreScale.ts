/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scoreScalesManagementCreateScoreScale } from "../../funcs/scoreScalesManagementCreateScoreScale.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateScoreScaleRequest$inboundSchema,
};

export const tool$scoreScalesManagementCreateScoreScale: ToolDefinition<
  typeof args
> = {
  name: "score-scales-management-create-score-scale",
  description: `Create a Score Scale

To create a new scoreScale. The responding system must return the set of sourcedIds that have been allocated to the newly created scoreScale records.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scoreScalesManagementCreateScoreScale(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
