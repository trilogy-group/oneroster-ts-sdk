/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demographicsManagementPostDemographics } from "../../funcs/demographicsManagementPostDemographics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostDemographicsRequest$inboundSchema.optional(),
};

export const tool$demographicsManagementPostDemographics: ToolDefinition<
  typeof args
> = {
  name: "demographics-management-post-demographics",
  description: `Create a new Demographic record

To create a new Demographic record. The responding system must return the set of sourcedIds that have been allocated to the newly created demographic record.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demographicsManagementPostDemographics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
