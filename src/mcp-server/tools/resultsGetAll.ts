/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resultsGetAll } from "../../funcs/resultsGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllResultsRequest$inboundSchema,
};

export const tool$resultsGetAll: ToolDefinition<typeof args> = {
  name: "results-get-all",
  description: `Get all Results

Get all of the results on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resultsGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
