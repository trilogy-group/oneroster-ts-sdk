/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assessmentResultsManagementGetAssessmentResult } from "../../funcs/assessmentResultsManagementGetAssessmentResult.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAssessmentResultRequest$inboundSchema,
};

export const tool$assessmentResultsManagementGetAssessmentResult:
  ToolDefinition<typeof args> = {
    name: "assessment-results-management-get-assessment-result",
    description: `Get an Assessment Result

Get a specific Assessment Result on the service provider. If the corresponding record cannot be located, the api will return a 404 error code and message 'Assessment result not found.'`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await assessmentResultsManagementGetAssessmentResult(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
