/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teachersManagementGetTeachersForClassInSchool } from "../../funcs/teachersManagementGetTeachersForClassInSchool.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTeachersForClassInSchoolRequest$inboundSchema,
};

export const tool$teachersManagementGetTeachersForClassInSchool: ToolDefinition<
  typeof args
> = {
  name: "teachers-management-get-teachers-for-class-in-school",
  description: `Get Teachers for a specific Class in a School

To get all Teachers for a Class in a School on the service provider. If the specified school and/or class cannot be identified within the service provider, the api will return a 404 error code and message 'School or class not found.'`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await teachersManagementGetTeachersForClassInSchool(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
