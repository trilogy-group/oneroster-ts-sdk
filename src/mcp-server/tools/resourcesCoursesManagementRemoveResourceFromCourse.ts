/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesCoursesManagementRemoveResourceFromCourse } from "../../funcs/resourcesCoursesManagementRemoveResourceFromCourse.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveResourceFromCourseRequest$inboundSchema,
};

export const tool$resourcesCoursesManagementRemoveResourceFromCourse:
  ToolDefinition<typeof args> = {
    name: "resources-courses-management-remove-resource-from-course",
    description: `Remove a resource from a course

To unassign an existing resource from the specified course. If the corresponding record cannot be located, the api will return a 404 error code and message 'Course or resource relationship not found.'`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await resourcesCoursesManagementRemoveResourceFromCourse(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
