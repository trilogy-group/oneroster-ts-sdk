/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { coursesGetResources } from "../../funcs/coursesGetResources.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetResourcesForCourseRequest$inboundSchema,
};

export const tool$coursesGetResources: ToolDefinition<typeof args> = {
  name: "courses-get-resources",
  description: `Get resources for a course

To get the collection of resources assigned to a specific course. If the corresponding record cannot be located, the api will return a 404 error code and message 'Course not found.'`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await coursesGetResources(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
