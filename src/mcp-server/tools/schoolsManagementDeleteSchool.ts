/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { schoolsManagementDeleteSchool } from "../../funcs/schoolsManagementDeleteSchool.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteSchoolRequest$inboundSchema,
};

export const tool$schoolsManagementDeleteSchool: ToolDefinition<typeof args> = {
  name: "schools-management-delete-school",
  description: `Delete a School

Perform a soft delete on a specific School on the service provider. The operation changes the status of the School to 'tobedeleted'.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await schoolsManagementDeleteSchool(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
