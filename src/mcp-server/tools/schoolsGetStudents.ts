/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { schoolsGetStudents } from "../../funcs/schoolsGetStudents.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStudentsForSchoolRequest$inboundSchema,
};

export const tool$schoolsGetStudents: ToolDefinition<typeof args> = {
  name: "schools-get-students",
  description: `Get all Students for a School

To get all Students for a School on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await schoolsGetStudents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
