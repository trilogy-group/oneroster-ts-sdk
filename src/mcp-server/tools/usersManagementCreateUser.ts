/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersManagementCreateUser } from "../../funcs/usersManagementCreateUser.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateUserRequest$inboundSchema,
};

export const tool$usersManagementCreateUser: ToolDefinition<typeof args> = {
  name: "users-management-create-user",
  description: `Create a new User

To create a new User on the service provider. The responding system must return the set of sourcedIds that have been allocated to the newly created user record.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersManagementCreateUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
