/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { academicSessionsManagementGetAcademicSession } from "../../funcs/academicSessionsManagementGetAcademicSession.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAcademicSessionRequest$inboundSchema,
};

export const tool$academicSessionsManagementGetAcademicSession: ToolDefinition<
  typeof args
> = {
  name: "academic-sessions-management-get-academic-session",
  description: `Get a specific Academic Session

Get a specific Academic Session on the service provider. If the corresponding record cannot be located, the api will return a 404 error code and message 'Academic session not found.'`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await academicSessionsManagementGetAcademicSession(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
