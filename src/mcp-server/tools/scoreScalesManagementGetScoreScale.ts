/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scoreScalesManagementGetScoreScale } from "../../funcs/scoreScalesManagementGetScoreScale.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetScoreScaleRequest$inboundSchema,
};

export const tool$scoreScalesManagementGetScoreScale: ToolDefinition<
  typeof args
> = {
  name: "score-scales-management-get-score-scale",
  description: `Get a Score Scale

Get a specific scoreScale on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scoreScalesManagementGetScoreScale(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
