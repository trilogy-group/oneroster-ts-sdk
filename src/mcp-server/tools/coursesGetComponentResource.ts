/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { coursesGetComponentResource } from "../../funcs/coursesGetComponentResource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetComponentResourceRequest$inboundSchema,
};

export const tool$coursesGetComponentResource: ToolDefinition<typeof args> = {
  name: "courses-get-component-resource",
  description: `Get Component Resource

Returns a specific component resource by sourcedId`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await coursesGetComponentResource(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
