/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demographicsGetAll } from "../../funcs/demographicsGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllDemographicsRequest$inboundSchema,
};

export const tool$demographicsGetAll: ToolDefinition<typeof args> = {
  name: "demographics-get-all",
  description: `Get all Demographic records

To get all Demographic records on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demographicsGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
