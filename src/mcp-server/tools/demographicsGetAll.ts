/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demographicsGetAll } from "../../funcs/demographicsGetAll.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$demographicsGetAll: ToolDefinition = {
  name: "demographics-get-all",
  description: `Get all demographics records

Returns a collection of all demographics records`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await demographicsGetAll(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
