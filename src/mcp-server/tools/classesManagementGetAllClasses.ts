/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesManagementGetAllClasses } from "../../funcs/classesManagementGetAllClasses.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllClassesRequest$inboundSchema,
};

export const tool$classesManagementGetAllClasses: ToolDefinition<typeof args> =
  {
    name: "classes-management-get-all-classes",
    description: `Get all Classes

To get all Classes on the service provider.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await classesManagementGetAllClasses(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
