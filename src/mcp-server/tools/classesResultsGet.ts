/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesResultsGet } from "../../funcs/classesResultsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetResultsForClassRequest$inboundSchema,
};

export const tool$classesResultsGet: ToolDefinition<typeof args> = {
  name: "classes-results-get",
  description: `Get results for a class

Returns a collection of results associated with a specific class.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classesResultsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
