/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { academicSessionsManagementPutAcademicSession } from "../../funcs/academicSessionsManagementPutAcademicSession.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutAcademicSessionRequest$inboundSchema,
};

export const tool$academicSessionsManagementPutAcademicSession: ToolDefinition<
  typeof args
> = {
  name: "academic-sessions-management-put-academic-session",
  description: `Update an Academic Session

To update an existing Academic Session. The sourcedId for the record to be updated is supplied by the requesting system.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await academicSessionsManagementPutAcademicSession(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
