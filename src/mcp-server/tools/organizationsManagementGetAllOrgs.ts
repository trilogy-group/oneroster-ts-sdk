/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsManagementGetAllOrgs } from "../../funcs/organizationsManagementGetAllOrgs.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllOrgsRequest$inboundSchema,
};

export const tool$organizationsManagementGetAllOrgs: ToolDefinition<
  typeof args
> = {
  name: "organizations-management-get-all-orgs",
  description: `Get all Organizations

To get all Organizations on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsManagementGetAllOrgs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
