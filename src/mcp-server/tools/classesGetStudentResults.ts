/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesGetStudentResults } from "../../funcs/classesGetStudentResults.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetResultsForStudentForClassRequest$inboundSchema,
};

export const tool$classesGetStudentResults: ToolDefinition<typeof args> = {
  name: "classes-get-student-results",
  description: `Get results for a specific student in a class

Returns a collection of results for a specific student within a class.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classesGetStudentResults(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
