/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesGet } from "../../funcs/classesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetClassRequest$inboundSchema,
};

export const tool$classesGet: ToolDefinition<typeof args> = {
  name: "classes-get",
  description: `Get a specific class

Returns a specific class by sourcedId`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
