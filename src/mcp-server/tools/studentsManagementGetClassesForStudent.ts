/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { studentsManagementGetClassesForStudent } from "../../funcs/studentsManagementGetClassesForStudent.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetClassesForStudentRequest$inboundSchema,
};

export const tool$studentsManagementGetClassesForStudent: ToolDefinition<
  typeof args
> = {
  name: "students-management-get-classes-for-student",
  description: `Get Classes for a Student

To get the set of Classes related to a specific Student. If the specified student cannot be identified within the service provider, the api will return a 404 error code and message 'Student not found.'`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await studentsManagementGetClassesForStudent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
