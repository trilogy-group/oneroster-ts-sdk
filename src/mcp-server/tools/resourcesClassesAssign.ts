/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesClassesAssign } from "../../funcs/resourcesClassesAssign.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AssignResourceToClassRequest$inboundSchema,
};

export const tool$resourcesClassesAssign: ToolDefinition<typeof args> = {
  name: "resources-classes-assign",
  description: `Assign a resource to a class

Assigns an existing resource to the specified class.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesClassesAssign(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
