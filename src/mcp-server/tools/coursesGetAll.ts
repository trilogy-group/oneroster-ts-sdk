/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { coursesGetAll } from "../../funcs/coursesGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllCoursesRequest$inboundSchema,
};

export const tool$coursesGetAll: ToolDefinition<typeof args> = {
  name: "courses-get-all",
  description: `Get All Courses

To get all Courses on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await coursesGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
