/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { schoolsGetClasses } from "../../funcs/schoolsGetClasses.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetClassesForSchoolRequest$inboundSchema,
};

export const tool$schoolsGetClasses: ToolDefinition<typeof args> = {
  name: "schools-get-classes",
  description: `Get all classes for a school

Returns a collection of classes for a specific school`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await schoolsGetClasses(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
