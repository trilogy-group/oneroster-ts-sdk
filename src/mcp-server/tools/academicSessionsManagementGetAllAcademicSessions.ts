/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { academicSessionsManagementGetAllAcademicSessions } from "../../funcs/academicSessionsManagementGetAllAcademicSessions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllAcademicSessionsRequest$inboundSchema,
};

export const tool$academicSessionsManagementGetAllAcademicSessions:
  ToolDefinition<typeof args> = {
    name: "academic-sessions-management-get-all-academic-sessions",
    description: `Get all Academic Sessions

To get all Academic Sessions on the service provider.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await academicSessionsManagementGetAllAcademicSessions(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
