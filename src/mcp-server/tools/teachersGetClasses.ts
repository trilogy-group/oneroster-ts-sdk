/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teachersGetClasses } from "../../funcs/teachersGetClasses.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetClassesForTeacherRequest$inboundSchema,
};

export const tool$teachersGetClasses: ToolDefinition<typeof args> = {
  name: "teachers-get-classes",
  description: `Get Classes for a Teacher

To get the set of Classes a Teacher is enrolled in. If the specified teacher cannot be identified within the service provider, the api will return a 404 error code and message 'Teacher not found.'`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teachersGetClasses(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
