/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersManagementGetUser } from "../../funcs/usersManagementGetUser.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUserRequest$inboundSchema,
};

export const tool$usersManagementGetUser: ToolDefinition<typeof args> = {
  name: "users-management-get-user",
  description: `Get a specific User

To get a specific User on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersManagementGetUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
