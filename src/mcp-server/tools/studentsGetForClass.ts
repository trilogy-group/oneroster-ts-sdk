/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { studentsGetForClass } from "../../funcs/studentsGetForClass.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStudentsForClassInSchoolRequest$inboundSchema,
};

export const tool$studentsGetForClass: ToolDefinition<typeof args> = {
  name: "students-get-for-class",
  description: `Get students for a specific class in a school

Returns a collection of students enrolled in a specific class within a school`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await studentsGetForClass(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
