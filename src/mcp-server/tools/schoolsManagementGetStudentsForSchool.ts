/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { schoolsManagementGetStudentsForSchool } from "../../funcs/schoolsManagementGetStudentsForSchool.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStudentsForSchoolRequest$inboundSchema,
};

export const tool$schoolsManagementGetStudentsForSchool: ToolDefinition<
  typeof args
> = {
  name: "schools-management-get-students-for-school",
  description: `Get all Students for a School

To get all Students for a School on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await schoolsManagementGetStudentsForSchool(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
