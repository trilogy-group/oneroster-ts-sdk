/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesUsersManagementRemoveResourceFromUser } from "../../funcs/resourcesUsersManagementRemoveResourceFromUser.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveResourceFromUserRequest$inboundSchema,
};

export const tool$resourcesUsersManagementRemoveResourceFromUser:
  ToolDefinition<typeof args> = {
    name: "resources-users-management-remove-resource-from-user",
    description: `Remove a resource from a user

To unassign an existing resource from the specified user. If the corresponding record cannot be located, the api will return a 404 error code and message 'User or resource relationship not found.'`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await resourcesUsersManagementRemoveResourceFromUser(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
