/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesGetScoreScales } from "../../funcs/classesGetScoreScales.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetScoreScalesForClassRequest$inboundSchema,
};

export const tool$classesGetScoreScales: ToolDefinition<typeof args> = {
  name: "classes-get-score-scales",
  description: `Get score scales for a class

Returns a collection of score scales associated with a specific class.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classesGetScoreScales(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
