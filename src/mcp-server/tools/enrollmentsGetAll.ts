/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { enrollmentsGetAll } from "../../funcs/enrollmentsGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllEnrollmentsRequest$inboundSchema,
};

export const tool$enrollmentsGetAll: ToolDefinition<typeof args> = {
  name: "enrollments-get-all",
  description: `Get all Enrollments

To get all Enrollments on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await enrollmentsGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
