/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { enrollmentsGetAll } from "../../funcs/enrollmentsGetAll.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$enrollmentsGetAll: ToolDefinition = {
  name: "enrollments-get-all",
  description: `Get all enrollments

Returns a collection of all enrollment records`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await enrollmentsGetAll(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
