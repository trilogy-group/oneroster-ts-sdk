/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demographicsGet } from "../../funcs/demographicsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetDemographicsRequest$inboundSchema,
};

export const tool$demographicsGet: ToolDefinition<typeof args> = {
  name: "demographics-get",
  description: `Get a specific demographics record

Returns a single demographics record by sourcedId`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demographicsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
