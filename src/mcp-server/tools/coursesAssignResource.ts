/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { coursesAssignResource } from "../../funcs/coursesAssignResource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AssignResourceToCourseRequest$inboundSchema,
};

export const tool$coursesAssignResource: ToolDefinition<typeof args> = {
  name: "courses-assign-resource",
  description: `Assign a resource to a course

Assigns an existing resource to the specified course.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await coursesAssignResource(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
