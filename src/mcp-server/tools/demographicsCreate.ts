/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demographicsCreate } from "../../funcs/demographicsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostDemographicsRequest$inboundSchema.optional(),
};

export const tool$demographicsCreate: ToolDefinition<typeof args> = {
  name: "demographics-create",
  description: `Create a new demographics record

Creates a new demographics record with the provided information`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demographicsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
