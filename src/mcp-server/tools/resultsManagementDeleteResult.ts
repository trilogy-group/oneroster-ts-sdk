/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resultsManagementDeleteResult } from "../../funcs/resultsManagementDeleteResult.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteResultRequest$inboundSchema,
};

export const tool$resultsManagementDeleteResult: ToolDefinition<typeof args> = {
  name: "results-management-delete-result",
  description: `Delete a Result

Perform a soft delete on a specific Result on the service provider. This operation changes the status of the Result to 'tobedeleted'.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resultsManagementDeleteResult(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
