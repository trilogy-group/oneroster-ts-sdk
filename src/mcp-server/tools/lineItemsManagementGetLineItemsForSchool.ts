/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lineItemsManagementGetLineItemsForSchool } from "../../funcs/lineItemsManagementGetLineItemsForSchool.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLineItemsForSchoolRequest$inboundSchema,
};

export const tool$lineItemsManagementGetLineItemsForSchool: ToolDefinition<
  typeof args
> = {
  name: "line-items-management-get-line-items-for-school",
  description: `Get Line Items for a School

Get the set of lineItems on the service provider for a specific school. If the corresponding record cannot be located, the api will return a 404 error code and message 'School not found.'`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lineItemsManagementGetLineItemsForSchool(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
