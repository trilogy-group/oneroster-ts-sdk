/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { academicSessionsUpdate } from "../../funcs/academicSessionsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutAcademicSessionRequest$inboundSchema,
};

export const tool$academicSessionsUpdate: ToolDefinition<typeof args> = {
  name: "academic-sessions-update",
  description: `Update an academic session

Updates an existing academic session by sourcedId`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await academicSessionsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
