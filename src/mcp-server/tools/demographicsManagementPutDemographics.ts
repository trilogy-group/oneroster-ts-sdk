/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demographicsManagementPutDemographics } from "../../funcs/demographicsManagementPutDemographics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutDemographicsRequest$inboundSchema,
};

export const tool$demographicsManagementPutDemographics: ToolDefinition<
  typeof args
> = {
  name: "demographics-management-put-demographics",
  description: `Update a Demographic record

To update an existing Demographic record. The sourcedId for the record to be updated is supplied by the requesting system.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demographicsManagementPutDemographics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
