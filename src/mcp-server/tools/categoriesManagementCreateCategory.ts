/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { categoriesManagementCreateCategory } from "../../funcs/categoriesManagementCreateCategory.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateCategoryRequest$inboundSchema,
};

export const tool$categoriesManagementCreateCategory: ToolDefinition<
  typeof args
> = {
  name: "categories-management-create-category",
  description: `Create a Category

To create a new Category. The responding system must return the set of sourcedIds that have been allocated to the newly created category record. A 'title' MUST be provided when creating a category.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await categoriesManagementCreateCategory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
