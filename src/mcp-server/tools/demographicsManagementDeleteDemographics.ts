/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { demographicsManagementDeleteDemographics } from "../../funcs/demographicsManagementDeleteDemographics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteDemographicsRequest$inboundSchema,
};

export const tool$demographicsManagementDeleteDemographics: ToolDefinition<
  typeof args
> = {
  name: "demographics-management-delete-demographics",
  description: `Delete a Demographic record

Perform a soft delete on a specific Demographic record on the service provider. The operation changes the status of the Demographic record to 'tobedeleted'.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await demographicsManagementDeleteDemographics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
