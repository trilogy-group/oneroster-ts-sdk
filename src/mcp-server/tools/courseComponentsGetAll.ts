/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { courseComponentsGetAll } from "../../funcs/courseComponentsGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllCourseComponentsRequest$inboundSchema,
};

export const tool$courseComponentsGetAll: ToolDefinition<typeof args> = {
  name: "course-components-get-all",
  description: `Get All Course Components

Returns a collection of all course components`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await courseComponentsGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
