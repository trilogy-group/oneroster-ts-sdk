/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { enrollmentsManagementGetAllEnrollments } from "../../funcs/enrollmentsManagementGetAllEnrollments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllEnrollmentsRequest$inboundSchema,
};

export const tool$enrollmentsManagementGetAllEnrollments: ToolDefinition<
  typeof args
> = {
  name: "enrollments-management-get-all-enrollments",
  description: `Get all Enrollments

To get all Enrollments on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await enrollmentsManagementGetAllEnrollments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
