/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { coursesManagementCreateComponentResource } from "../../funcs/coursesManagementCreateComponentResource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateComponentResourceRequest$inboundSchema.optional(),
};

export const tool$coursesManagementCreateComponentResource: ToolDefinition<
  typeof args
> = {
  name: "courses-management-create-component-resource",
  description: `Create Component Resource

To create a new Component Resource. The responding system must return the set of sourcedIds that have been allocated to the newly created componentResource record.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await coursesManagementCreateComponentResource(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
