/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lineItemsList } from "../../funcs/lineItemsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllLineItemsRequest$inboundSchema,
};

export const tool$lineItemsList: ToolDefinition<typeof args> = {
  name: "line-items-list",
  description: `Get all Line Items

Get all of the Line Items on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lineItemsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
