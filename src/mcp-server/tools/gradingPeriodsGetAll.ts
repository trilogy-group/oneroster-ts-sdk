/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { gradingPeriodsGetAll } from "../../funcs/gradingPeriodsGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllGradingPeriodsRequest$inboundSchema,
};

export const tool$gradingPeriodsGetAll: ToolDefinition<typeof args> = {
  name: "grading-periods-get-all",
  description: `Get all Grading Periods

To get all Grading Periods on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await gradingPeriodsGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
