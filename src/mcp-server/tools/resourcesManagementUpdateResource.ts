/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesManagementUpdateResource } from "../../funcs/resourcesManagementUpdateResource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateResourceRequest$inboundSchema,
};

export const tool$resourcesManagementUpdateResource: ToolDefinition<
  typeof args
> = {
  name: "resources-management-update-resource",
  description: `Update an existing Resource

To update an existing resource. The sourcedId for the record to be updated is supplied by the requesting system.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resourcesManagementUpdateResource(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
