/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { enrollmentsManagementGetEnrollment } from "../../funcs/enrollmentsManagementGetEnrollment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetEnrollmentRequest$inboundSchema,
};

export const tool$enrollmentsManagementGetEnrollment: ToolDefinition<
  typeof args
> = {
  name: "enrollments-management-get-enrollment",
  description: `Get a specific Enrollment

Get a specific Enrollment on the service provider. If the corresponding record cannot be located, the api will return a 404 error code and message 'Enrollment not found.'`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await enrollmentsManagementGetEnrollment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
