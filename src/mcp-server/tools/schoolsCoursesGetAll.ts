/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { schoolsCoursesGetAll } from "../../funcs/schoolsCoursesGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCoursesForSchoolRequest$inboundSchema,
};

export const tool$schoolsCoursesGetAll: ToolDefinition<typeof args> = {
  name: "schools-courses-get-all",
  description: `Get all courses for a school

Returns a collection of courses for a specific school`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await schoolsCoursesGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
