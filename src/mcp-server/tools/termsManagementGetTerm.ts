/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { termsManagementGetTerm } from "../../funcs/termsManagementGetTerm.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTermRequest$inboundSchema,
};

export const tool$termsManagementGetTerm: ToolDefinition<typeof args> = {
  name: "terms-management-get-term",
  description: `Get a specific Term

To get a specific Term on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await termsManagementGetTerm(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
