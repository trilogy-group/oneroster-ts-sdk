/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesCreate } from "../../funcs/classesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateClassRequest$inboundSchema.optional(),
};

export const tool$classesCreate: ToolDefinition<typeof args> = {
  name: "classes-create",
  description: `Create a new class

Creates a new class with the provided data`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
