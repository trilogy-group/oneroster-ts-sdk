/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classesManagementGetResultsForStudentForClass } from "../../funcs/classesManagementGetResultsForStudentForClass.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetResultsForStudentForClassRequest$inboundSchema,
};

export const tool$classesManagementGetResultsForStudentForClass: ToolDefinition<
  typeof args
> = {
  name: "classes-management-get-results-for-student-for-class",
  description: `Get Results for a Student for a Class

Get the set of results on the service provider for a specific student and for a specific class. If the corresponding record cannot be located, the api will return a 404 error code and message 'Class or student not found.'`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await classesManagementGetResultsForStudentForClass(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
