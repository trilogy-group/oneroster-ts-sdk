/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { enrollmentsCreate } from "../../funcs/enrollmentsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateEnrollmentRequest$inboundSchema.optional(),
};

export const tool$enrollmentsCreate: ToolDefinition<typeof args> = {
  name: "enrollments-create",
  description: `Create a new Enrollment

To create a new Enrollment. The responding system must return the set of sourcedIds that have been allocated to the newly created enrollment record.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await enrollmentsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
