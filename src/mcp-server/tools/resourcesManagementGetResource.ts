/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesManagementGetResource } from "../../funcs/resourcesManagementGetResource.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetResourceRequest$inboundSchema,
};

export const tool$resourcesManagementGetResource: ToolDefinition<typeof args> =
  {
    name: "resources-management-get-resource",
    description: `Get a specific Resource

To get a specific resource by sourcedId. If the corresponding record cannot be located, the api will return a 404 error code and message 'Resource not found.'`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await resourcesManagementGetResource(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
