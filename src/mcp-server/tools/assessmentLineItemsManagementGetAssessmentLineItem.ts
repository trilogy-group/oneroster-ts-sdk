/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assessmentLineItemsManagementGetAssessmentLineItem } from "../../funcs/assessmentLineItemsManagementGetAssessmentLineItem.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAssessmentLineItemRequest$inboundSchema,
};

export const tool$assessmentLineItemsManagementGetAssessmentLineItem:
  ToolDefinition<typeof args> = {
    name: "assessment-line-items-management-get-assessment-line-item",
    description: `Get an Assessment Line Item

Get a specific Assessment Line Item on the service provider. If the corresponding record cannot be located, the api will return a 404 error code and message 'Assessment line item not found.'`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await assessmentLineItemsManagementGetAssessmentLineItem(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
