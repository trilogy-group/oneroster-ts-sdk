/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { termsGetGradingPeriods } from "../../funcs/termsGetGradingPeriods.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGradingPeriodsForTermRequest$inboundSchema,
};

export const tool$termsGetGradingPeriods: ToolDefinition<typeof args> = {
  name: "terms-get-grading-periods",
  description: `Get Grading Periods for a Term

To get the set of Grading Periods related to a specific Term. If the specified Term cannot be identified within the service provider, the api will return a 404 error code and message 'Term not found.'`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await termsGetGradingPeriods(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
