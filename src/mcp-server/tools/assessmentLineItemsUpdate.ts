/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assessmentLineItemsUpdate } from "../../funcs/assessmentLineItemsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAssessmentLineItemRequest$inboundSchema,
};

export const tool$assessmentLineItemsUpdate: ToolDefinition<typeof args> = {
  name: "assessment-line-items-update",
  description:
    `The REST update request message for the updateAssessmentLineItem() API call.

To update an existing Assessment Line Item. The sourcedId for the record to be updated is supplied by the requesting system.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await assessmentLineItemsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
