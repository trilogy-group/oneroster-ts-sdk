/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsGetAll } from "../../funcs/organizationsGetAll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllOrgsRequest$inboundSchema,
};

export const tool$organizationsGetAll: ToolDefinition<typeof args> = {
  name: "organizations-get-all",
  description: `Get all Organizations

To get all Organizations on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsGetAll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
