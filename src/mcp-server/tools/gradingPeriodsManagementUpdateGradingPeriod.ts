/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { gradingPeriodsManagementUpdateGradingPeriod } from "../../funcs/gradingPeriodsManagementUpdateGradingPeriod.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateGradingPeriodRequest$inboundSchema,
};

export const tool$gradingPeriodsManagementUpdateGradingPeriod: ToolDefinition<
  typeof args
> = {
  name: "grading-periods-management-update-grading-period",
  description: `Update a Grading Period

To update an existing Grading Period. The sourcedId for the record to be updated is supplied by the requesting system.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await gradingPeriodsManagementUpdateGradingPeriod(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
