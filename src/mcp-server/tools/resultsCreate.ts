/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resultsCreate } from "../../funcs/resultsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateResultRequest$inboundSchema,
};

export const tool$resultsCreate: ToolDefinition<typeof args> = {
  name: "results-create",
  description: `Create a new result

Creates a new result with the provided information.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resultsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
