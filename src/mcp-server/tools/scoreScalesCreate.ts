/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scoreScalesCreate } from "../../funcs/scoreScalesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateScoreScaleRequest$inboundSchema,
};

export const tool$scoreScalesCreate: ToolDefinition<typeof args> = {
  name: "score-scales-create",
  description: `Create a new score scale

Creates a new score scale with the provided information.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scoreScalesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
