/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assessmentResultsManagementDeleteAssessmentResult } from "../../funcs/assessmentResultsManagementDeleteAssessmentResult.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteAssessmentResultRequest$inboundSchema,
};

export const tool$assessmentResultsManagementDeleteAssessmentResult:
  ToolDefinition<typeof args> = {
    name: "assessment-results-management-delete-assessment-result",
    description: `Delete an Assessment Result

Perform a soft delete on a specific Assessment Result on the service provider. This operation changes the status of the Assessment Result to 'tobedeleted'.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await assessmentResultsManagementDeleteAssessmentResult(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
