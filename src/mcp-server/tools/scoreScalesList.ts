/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scoreScalesList } from "../../funcs/scoreScalesList.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$scoreScalesList: ToolDefinition = {
  name: "score-scales-list",
  description: `Get all score scales

Returns a collection of all score scales.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await scoreScalesList(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
