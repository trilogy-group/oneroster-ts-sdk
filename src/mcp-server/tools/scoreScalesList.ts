/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scoreScalesList } from "../../funcs/scoreScalesList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllScoreScalesRequest$inboundSchema,
};

export const tool$scoreScalesList: ToolDefinition<typeof args> = {
  name: "score-scales-list",
  description: `Get all Score Scales

Get all of the ScoreScales on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await scoreScalesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
