/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { schoolsUpdate } from "../../funcs/schoolsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateSchoolRequest$inboundSchema,
};

export const tool$schoolsUpdate: ToolDefinition<typeof args> = {
  name: "schools-update",
  description: `Update a school

Updates an existing school`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await schoolsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
