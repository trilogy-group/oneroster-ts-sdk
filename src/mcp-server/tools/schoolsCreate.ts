/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { schoolsCreate } from "../../funcs/schoolsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateSchoolRequest$inboundSchema.optional(),
};

export const tool$schoolsCreate: ToolDefinition<typeof args> = {
  name: "schools-create",
  description: `Create a new school

Creates a new school`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await schoolsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
