/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsManagementGetOrg } from "../../funcs/organizationsManagementGetOrg.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOrgRequest$inboundSchema,
};

export const tool$organizationsManagementGetOrg: ToolDefinition<typeof args> = {
  name: "organizations-management-get-org",
  description: `Get a specific Organization

Get a specific Organization on the service provider. If the corresponding record cannot be located, the api will return a 404 error code and message 'Organization not found.'`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsManagementGetOrg(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
