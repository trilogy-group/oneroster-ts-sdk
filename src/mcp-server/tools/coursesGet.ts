/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { coursesGet } from "../../funcs/coursesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCourseRequest$inboundSchema,
};

export const tool$coursesGet: ToolDefinition<typeof args> = {
  name: "courses-get",
  description: `Get Course

Returns a specific course by sourcedId`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await coursesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
