/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { enrollmentsDelete } from "../../funcs/enrollmentsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteEnrollmentRequest$inboundSchema,
};

export const tool$enrollmentsDelete: ToolDefinition<typeof args> = {
  name: "enrollments-delete",
  description: `Delete an enrollment

Deletes the enrollment with the specified sourcedId`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await enrollmentsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
