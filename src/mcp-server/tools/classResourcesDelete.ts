/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { classResourcesDelete } from "../../funcs/classResourcesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveResourceFromClassRequest$inboundSchema,
};

export const tool$classResourcesDelete: ToolDefinition<typeof args> = {
  name: "class-resources-delete",
  description: `Remove a resource from a class

To unassign an existing resource from the specified class. If the corresponding record cannot be located, the api will return a 404 error code and message 'Class or resource relationship not found.'`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await classResourcesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
