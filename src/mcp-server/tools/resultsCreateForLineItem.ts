/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resultsCreateForLineItem } from "../../funcs/resultsCreateForLineItem.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateResultForLineItemRequest$inboundSchema,
};

export const tool$resultsCreateForLineItem: ToolDefinition<typeof args> = {
  name: "results-create-for-line-item",
  description: `Create a new result for a line item

Creates a new result associated with the specified line item.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await resultsCreateForLineItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
