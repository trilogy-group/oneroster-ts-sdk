/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { schoolsManagementGetAllSchools } from "../../funcs/schoolsManagementGetAllSchools.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllSchoolsRequest$inboundSchema,
};

export const tool$schoolsManagementGetAllSchools: ToolDefinition<typeof args> =
  {
    name: "schools-management-get-all-schools",
    description: `Get all Schools

To get all Schools on the service provider.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await schoolsManagementGetAllSchools(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
