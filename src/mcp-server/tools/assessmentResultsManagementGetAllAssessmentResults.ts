/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assessmentResultsManagementGetAllAssessmentResults } from "../../funcs/assessmentResultsManagementGetAllAssessmentResults.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllAssessmentResultsRequest$inboundSchema,
};

export const tool$assessmentResultsManagementGetAllAssessmentResults:
  ToolDefinition<typeof args> = {
    name: "assessment-results-management-get-all-assessment-results",
    description: `Get all Assessment Results

Get all of the Assessment Results on the service provider.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await assessmentResultsManagementGetAllAssessmentResults(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
