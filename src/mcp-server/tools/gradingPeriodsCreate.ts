/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { gradingPeriodsCreate } from "../../funcs/gradingPeriodsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateGradingPeriodRequest$inboundSchema.optional(),
};

export const tool$gradingPeriodsCreate: ToolDefinition<typeof args> = {
  name: "grading-periods-create",
  description: `Create a new grading period

Creates a new grading period`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await gradingPeriodsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
