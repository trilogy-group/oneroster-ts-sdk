/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsManagementUpdateOrg } from "../../funcs/organizationsManagementUpdateOrg.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateOrgRequest$inboundSchema,
};

export const tool$organizationsManagementUpdateOrg: ToolDefinition<
  typeof args
> = {
  name: "organizations-management-update-org",
  description: `Update an Organization

To update an existing Organization. The sourcedId for the record to be updated is supplied by the requesting system.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsManagementUpdateOrg(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
