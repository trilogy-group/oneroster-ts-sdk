/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teachersManagementGetAllTeachers } from "../../funcs/teachersManagementGetAllTeachers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAllTeachersRequest$inboundSchema,
};

export const tool$teachersManagementGetAllTeachers: ToolDefinition<
  typeof args
> = {
  name: "teachers-management-get-all-teachers",
  description: `Get all Teachers

To get all Teachers on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teachersManagementGetAllTeachers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
