/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersGetClasses } from "../../funcs/usersGetClasses.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetClassesForUserRequest$inboundSchema,
};

export const tool$usersGetClasses: ToolDefinition<typeof args> = {
  name: "users-get-classes",
  description: `Get classes for a user

Returns a collection of classes associated with the user.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersGetClasses(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
