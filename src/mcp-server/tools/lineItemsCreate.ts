/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lineItemsCreate } from "../../funcs/lineItemsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateLineItemRequest$inboundSchema,
};

export const tool$lineItemsCreate: ToolDefinition<typeof args> = {
  name: "line-items-create",
  description: `Create a Line Item

To create a new Line Item. The responding system must return the set of sourcedIds that have been allocated to the newly created Line Item records.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lineItemsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
