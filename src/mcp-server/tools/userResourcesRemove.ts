/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userResourcesRemove } from "../../funcs/userResourcesRemove.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveResourceFromUserRequest$inboundSchema,
};

export const tool$userResourcesRemove: ToolDefinition<typeof args> = {
  name: "user-resources-remove",
  description: `Remove a resource from a user

Removes the specified resource from the user.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userResourcesRemove(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
